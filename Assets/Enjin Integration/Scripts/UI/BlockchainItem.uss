/* ItemDetails has no label (the item name is now its own column); collapse the
   empty field label so the editable amount uses the full field width. */
#ItemDetails .unity-base-field__label {
    display: none;
}
